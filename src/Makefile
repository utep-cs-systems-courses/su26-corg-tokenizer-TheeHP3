# compiler flags.  -g for debug, -O3 for optimization
CFLAGS=-g -O3

compile: uimain.o tokenizer.o
	gcc -o compile uimain.o tokenizer.o

uimain.o:
	gcc -c uimain.c -I.

tokenizer.o:
	gcc -c tokenizer.c -I.

# deletes files generated by compilation
clean:
	rm -f *.o compile
